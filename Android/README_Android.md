This is the file about information about the Android version of the program Numbers.

Changeable variables in the "MainPage.xaml.cs" file:
- `int maxNumber`, `bufferedStreamSize`, `margin`, `internalStreamBuffer`, `fileArrayPoolSize`, `fileMargin`
- `string timestamp`, `targetDir`, `fileName`;

Changeable in Android "Numbers_Android.csproj" file:
- `<ApplicationDisplayVersion>`, `<ApplicationVersion>`, `<VersionOnly>`;